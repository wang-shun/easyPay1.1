<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
    >

    <ImageView
        android:visibility="gone"
        android:layout_centerVertical="true"
        android:id="@+id/dropview_image_left"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="right"
        android:paddingRight="@dimen/dp5"
        />

    <RelativeLayout
        android:layout_centerVertical="true"
        android:id="@+id/dropview_ly"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/dropview_image_left"
        android:background="@drawable/border_black_normal"
        android:padding="@dimen/dp5">

        <com.acewill.ordermachine.widget.CommonEditText
            android:id="@+id/dropview_edit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@null"
            android:layout_centerVertical="true"
            android:hint="请输入终端授权码"
            android:singleLine="true"/>


        <ImageView
            android:layout_centerVertical="true"
            android:id="@+id/dropview_image"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_alignParentRight="true"
            android:layout_gravity="right"
            android:visibility="gone"
            />
    </RelativeLayout>
</RelativeLayout>
